import React, { Fragment } from 'react';

import MainCart from '../components/Cart/MainCart/MainCart';

const Cart = () => {
    return (
        <Fragment>
            <MainCart />
        </Fragment>
    );
};

export default Cart;