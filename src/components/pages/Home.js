import React, { Fragment } from 'react';
import HomePage from '../HomePage/HomePage';

const Home = () => {
    return (
        <Fragment>
            <HomePage />
        </Fragment>
    );
};

export default Home;